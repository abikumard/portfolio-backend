spring.application.name=portfoliobackend

server.port=${PORT:8080}

spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.sql.init.mode=never

logging.level.org.springframework.jdbc.core=INFO
logging.level.root=INFO
logging.file.name=logs/portfolio.log

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=abikumarr003@gmail.com
spring.mail.password=mbvo frsw ykdx nhpi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.data.redis.host=${REDISHOST:localhost}
spring.data.redis.port=${REDISPORT:6379}

server.servlet.session.timeout=30m

jwt.secret=MyPortfolioSecretKeyForJWT2026VerySecureSecretKeyForProduction
jwt.expiration=86400000

file.upload-dir=uploads/